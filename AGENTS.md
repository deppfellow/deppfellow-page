# Deppfellow Page

Site facing repository for personal site, rendering article from wiki-facing repository.
The project local wiki is in `/mnt/c/Users/%USERPROFILE%/Documents/vaults/deppfellow-wiki`.

## Check & Lint

After every work done, work should be checked and linted first before PR/MR is created.
Run `npm run check` -> `npm run lint` -> `npm run format`.
