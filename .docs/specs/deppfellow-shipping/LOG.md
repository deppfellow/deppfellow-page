E-01 [event] 2026-09-12 — grilling initialized; slug confirmed "deppfellow-shipping".
D-01 [decision] 2026-09-12 — One end-to-end epic "deppfellow-shipping": from the four content surfaces and the 404 page through RSS, Pagefind, the GitHub Actions build and the Pages deploy at the domain root (ADR-0011). Single epic, one ticket per actionable task.
D-02 [decision] 2026-09-12 — Article reading pages carry a floating action button (FAB): default collapsed at the bottom-right; on click it expands to an in-page heading index covering h2 and h3 only. h1 is the article title and is never listed in the FAB.
C-01 [constraint] 2026-09-12 — Article front matter uses `description`, `created`, `tags`; the retired `origin` field is not rendered. The loader, fixtures, and note schema migrate description to the article's metadata/lede.
D-03 [decision] 2026-09-12 — Articles index renders strictly date + title per the Morg reference; no description line.
D-04 [decision] 2026-09-12 — Reading page metadata: "Back to Articles" link, tags as plain text (not links), and prev/next chained by `created` within the category.
D-05 [decision] 2026-09-12 — Pagefind ships its asset bundle plus a minimal progressive-enhancement script scoped to a search surface; the reading pages themselves stay script-free.
D-06 [decision] 2026-09-12 — One `build.yml`: shallow-clone the wiki at HEAD as WIKI_PATH, `astro build` under the registry boundary, a grep-level sanity gate, deploy from `dist/` to GitHub Pages. Wiki pushes fire it via `repository_dispatch`; site-repo pushes run it directly on `push`.
D-07 [decision] 2026-09-12 — The client-only Markdown import (browser-read, `obsidian://new` URI handoff into a selected registry-listed category, no upload, manual git push preserved, clipboard-copy fallback for large files) is in scope as its own ticket.
D-08 [decision] 2026-09-12 — Pagefind search lives on a dedicated `/search` page; home, list, and reading pages stay script-free.
D-09 [decision] 2026-09-12 — `/rss.xml` covers Articles and Projects only (Logs excluded), ordered by `created` newest-first, capped at 50, abs links, title/dates/description (the new `description` field as the item summary).
D-10 [decision] 2026-09-12 — Logs index: date groups newest-first; each row shows date, excerpt, tags; site derives prev/next ordering from `created`.
D-11 [decision] 2026-09-12 — Reading page header: title, then a 65ch description-lede, then date + tags. The description is not on the Articles index and is never in the FAB.
D-12 [decision] 2026-09-12 — Import flow: rule-band "Import" on a script-scoped page, `<input type="file">` client-only (no upload), opens the `obsidian://` URI with vault + category + filename + encoded content, clipboard-copy fallback for length limits; manual git push stays the publication step.
Q-01 [question] 2026-09-12 — Running: which Pagefind page files and the exact `obsidian://new` URI string grow from the ticket contracts, not this interview.
D-13 [decision] 2026-09-12 — Reading page renders core Markdown, callouts, tables, fenced code (hairline frame), blockquotes AND math. The MVP block is core + callouts + math; tables/fenced-code/blockquotes ship alongside. Math is build-time rendered (no client math script), consistent with the zero-JS reading pages.
C-03 [constraint] 2026-09-12 — The MVP rendering demonstration must show core Markdown, callouts, and math working together; everything else in the reading surface is secondary polish behind those three.
D-14 [decision] 2026-09-12 — Deploy gate: the built site must contain an index per registered category, `rss.xml`, `/search`, and `404.html`, and the loader must have read at least one Article; otherwise the workflow fails before deploy-pages.
D-15 [decision] 2026-09-12 — Article route stays `/articles/<slug>/`; a slug rename in the vault is a new article unless a forward is explicitly carried. Treat renames as content for this epic.
D-16 [decision] 2026-09-12 — Minor JavaScript is acceptable; the constraint is "not bloated", not "zero". Script stays scoped: a rule-band Import button (scripted) and a /search page (Pagefind) are fine; the reading pages carry no page code. No framework runtime on the page; enhancement script is small and scoped.
E-02 [event] 2026-09-12 — derivation gate: frontier empty, human confirmed shared understanding ("proceed").
D-17 [decision] 2026-09-12 — (council F1) The FAB is the one enumerated JavaScript exception on reading pages; the Constraint is amended to allow it. The script-count Testing seam becomes whitelist-mode: scripts valid only on /search, /import, and reading pages (the FAB).
D-18 [decision] 2026-09-12 — (council F3) ADR-0005 V1 scope is in scope: wikilink resolution (against the vault tree + registry, degrade-to-plain-text for unlisted), images from_assets/ via the build-time sharp pipeline, footnotes, task-list checkboxes, and inline tags. Wikilink resolution and the JSON catalog graph neighbors share one pass.
D-19 [decision] 2026-09-12 — (council F6) Repo rename to deppfellow/deppfellow.github.io and enabling Pages-from-GitHub-Actions are human-owned GitHub-settings actions; they are sequenced BEFORE any ticket that bakes absolute URLs.
D-20 [decision] 2026-09-12 — (council F7/F12) Summary precedence: front-matter `description` > `## Goal` section fallback > omit. A note lacking both renders without a summary but never fails the whole build; it is logged per-note. The loader no longer hard-fails the build on a single malformed note; per-note skip with a logged warning.
D-21 [decision] 2026-09-12 — (council F8) Detail pages ship for ALL categories this epic: /articles/<slug>, /projects/<slug>, /logs/<date>. RSS Projects links resolve to /projects/<slug>.
D-22 [decision] 2026-09-12 — (council F9) The deploy gate asserts against a build-emitted manifest (JSON listing routes + the loader's Article count), not grep of the HTML for strings. The loader throws if the build reads zero Articles.
D-23 [decision] 2026-09-12 — (council F10) /search gets an entry in the amended rule band (a search link), owned by T10.
D-24 [decision] 2026-09-12 — (council F11) A sitemap.xml is generated per ADR-0011, added as a REQ.
D-25 [decision] 2026-09-12 — (council F13) The wiki clone checks out the default branch explicitly (repository_dispatch carries no ref).
D-26 [decision] 2026-09-12 — (council F14) T6's contract covers FAB edge cases: empty h2/h3 index, heading scroll offset, keyboard/ESC/focus-return (disclosure widget).
D-27 [decision] 2026-09-12 — (council F15) Import byte cap pinned at the ADR-0001 precedent (~60KB encoded); the observable threshold behavior belongs in T11's contract.
Q-02 [question] 2026-09-12 — (council F2, escalated) Dispatch trigger model: fine-grained PAT secret on the wiki + an owned wiki-side workflow ticket, OR an ownerless scheduled/polling trigger that keeps "no secrets" true. Human arbitration at quiz gate.
Q-03 [question] 2026-09-12 — (council F4) Tag link policy: restore chip-linking on a surface (supersede D-04) or keep plain text and record ADR-0008 supersession. Human arbitration at quiz gate.
Q-04 [question] 2026-09-12 — (council F5) Deploy gate when the vault has zero Articles: hard-fail (block deploy) or staged warn-not-fail until first content lands. Human arbitration at quiz gate.
D-28 [decision] 2026-09-12 — (council F2 / resolves Q-02) The dispatch secret is owned, option (a). A wiki-side workflow ticket ships in this epic: a wiki-repo workflow that fires repository_dispatch, plus a fine-grained PAT (contents: write on the site repo) or GitHub App token stored as a wiki-repo secret, with a documented setup, rotation note, and a test-dispatch verification. ADR-0007's "No secrets" consequence is amended: tokenless applies to the clone only, not the trigger.
D-29 [decision] 2026-09-12 — (council F5 / resolves Q-04) The deploy gate is staged, option (b). Route presence (per-category index, rss.xml, search, 404, sitemap) is required and fails the job; zero Articles is a warn-not-fail until first content lands. No human-authored first-Article slice is added to this epic.
D-30 [decision] 2026-09-12 — (council F4 / resolves Q-03) Tag pages are chip-linked, option (a). Reading-page and log-row tags render as square, label-caps links to /tags/<tag> (no pills, no fills, per the Plates world); D-04's "tags as plain text" is superseded, and ADR-0008's chip-linking consequence stands.
D-31 [decision] 2026-09-12 — (council F1/REQ-07) The Constraint is amended: reading pages may ship the FAB script; ordinary list and home pages remain script-free. The script-count check is whitelist-mode (/search, /import, reading pages).
D-32 [decision] 2026-09-12 — (council F3/REQ-04) REQ-04 is expanded to ADR-0005's full V1 scope: wikilinks resolved against the vault tree and registry with degrade-to-plain-text, images from _assets/ through the build-time sharp pipeline, footnotes, task-list checkboxes, and inline #tags.
D-33 [decision] 2026-09-12 — (council F7/F12/REQ-02) Summary precedence: `description` > `## Goal` section > omit, for Projects rows, reading-page ledes, and RSS summaries. A note lacking both renders without a summary; malformed front matter is a per-note skip with a logged warning, never a whole-build failure.
D-34 [decision] 2026-09-12 — (council F13/D-06) The receiver workflow lives on the default branch and checks out the wiki's default branch explicitly; repository_dispatch carries no ref.
D-35 [decision] 2026-09-12 — (resolves Q-01) Pagefind generated page files and the `obsidian://new` URI string are fixed inside T12's and T13's contracts respectively; no spec-level value.
D-36 [decision] 2026-09-12 — (resolves Q-02) Resolved by D-28: the dispatch secret is owned via a wiki-side token.
D-37 [decision] 2026-09-12 — (resolves Q-03) Resolved by D-30: tags are chip-linked.
D-38 [decision] 2026-09-12 — (resolves Q-04) Resolved by D-29: the gate is staged, warn on zero Articles.
E-03 [event] 2026-09-12 — approved: human confirmed the amended spec and the 17-slice decomposition ("Approved"). td is initialized at the repo root (.todos/), so the epic/issue/dep/label layer applies.
D-39 [decision] 2026-09-13 — (pre-flight recast) T5's Gate became a session-runnable unit test over the resolution pass (its route-grep Gate was not fillable until T6 existed). T16 was split: the workflow file and docs stay agent-owned; token creation, secret storage, and the live test dispatch moved to human-owned T17. Ownership updated in Spec §8 and both ticket headers; no contract was thinned.
D-40 [decision] 2026-09-13 — Toolchain baseline (ADR-0014): Astro 7.3.2 and Tailwind 4.3.3, with `@astrojs/markdown-remark` pinned and `markdown.processor: unified()` set explicitly, because Astro 7 defaults to Sätteri and T5's resolution pass is a remark/rehype pipeline. Spacing between inline elements must be declared in classes, never inherited from source whitespace, because `compressHTML` now defaults to `'jsx'`. The RuleBand category links were changed to `inline-flex items-baseline` to conform. Added as slice T0, blocking T1.
E-04 [event] 2026-09-13 — T0 verified complete: build passes on Astro 7.3.2 + Tailwind 4.3.3 (2 pages, 19 fixture notes, custom loader `renderMarkdown` intact); frozen Astro 5 vs Astro 7 builds show 0 differing pixels at 1440x900 and 390x844 with identical computed geometry and byte-identical CSS; capture tool now parks the cursor for determinism.
D-41 [decision] 2026-09-14 — (T1 review round 3-5, user-approved) Summary-precedence scope finalization for the loader: wrong-typed front matter is malformed and rejected, never coerced — `created` accepts only string or Date (gray-matter already parses a bare YAML date to a Date; a number would silently coerce to an unrelated epoch date), `tags` must be `string[]` when present, `description` must be string when present. A multi-paragraph `## Goal` body flattens to one line containing all of it (the summary is single-line in shape, not first-line-truncated). Deferred OUT of T1 by product decision and documented verbatim on the ticket: an empty `## Goal` heading followed by another heading over-consumes the separating blank line (yields the next heading's text as the summary; the pre-T1 base was also wrong here, yielding the bare heading), and a `##` inside a fenced code block inside a Goal body is treated as a section boundary. Both ride with the downstream summary-surface tickets (T2/T3).
E-05 [event] 2026-09-14 — T1 (td-bffd30) CLOSED on the recorded independent approval (session ses_847c6b, PASS on attempt 5): L-AC-01..06, Gate, tsc, astro check, worker 19/19, tier-2 25/25, Preserved Invariants all pass. Uncommitted work sits on branch `wt/td-bffd30-migrate-description-summary` in the herdr worktree; landing the commit is the next action before T5/T2/T3 start.
D-42 [decision] 2026-09-14 — (ownership recast, human) The epic moves to human-owned implementation: T0-T16 recast from agent-owned to human-owned across Spec §8, every ticket header, and the td labels. T0 (td-a53be3) and T1 (td-bffd30) reopened, since their closed state describes agent-built code that is retired with this decision. The full agent build is preserved on branch `archive/agent-build`; `dev` resets to governance artifacts, fixtures, and design references pending a human scaffold from scratch. The human may amend reopened contracts where the from-scratch scaffold invalidates their baselines (T0's frozen-Astro-5 pixel comparison has no baseline anymore); no other contract is thinned.
E-06 [event] 2026-09-14 — Implementation reset executed on `dev`: `src/`, `astro.config.mjs`, `tsconfig.json`, `package.json`, `package-lock.json`, and `scripts/review-capture.mjs` removed; `capture-references.mjs`, `fixtures/`, `.docs/`, `.pi/skills/verify-deppfellow-page/`, and the td ledger kept. `archive/agent-build` holds the complete agent build (T0+T1 era, through the verify-skill commits).
D-43 [decision] 2026-09-16 — (user product decision) The summary fallback heading is renamed `## Goal` → `## Objective`. Precedence is unchanged: front-matter `description` > `## Objective` section > omit (amends REQ-02's wording; D-20/D-33 rationale stands). Applied to SPEC.md (REQ-02, Implementation Decisions, T1 slice row), T1 and T3 ticket contracts, the verify skill's feature map, PRODUCT.md's template line ("Projects carry `Status`, `Objective`, `Notes`"), and fixtures: the three Projects notes and the `goal-only` fixture (renamed to `objective-only` with title and body updated). Historical entries (D-20/D-33/D-41, party log) keep `## Goal` verbatim. Follow-up outside this repo: the wiki's `_templates/` and existing Projects notes must rename their Goal sections for the fallback to fire there.
D-44 [decision] 2026-09-19 — (T6 dispatch prep, human) T6 (td-9a444b) recast to agent-owned for agent implementation. Pre-flight found two fillability gaps, recast without thinning any other contract line: (1) the Gate's route was unfillable as written because slugs derive from the filename stem (vault.ts), so the fixture note's route is /articles/memory-layers-for-long-horizon-agents/, not /articles/memory-layers/; Gate and Examples now use the real slug. (2) No fixture body carried the callout/math elements L-AC-04 asserts, so the Interface Contract gains an explicit fixture-scope line: the memory-layers-for-long-horizon-agents body gains the demonstration elements (one note, build count 21/22/1 unchanged). The ticket's verifier dispatches on neuralwatt/deepseek-v4.1-flash-flex (different family than the worker).
D-45 [decision] 2026-09-20 — (T6 verifier dispatch, human) D-44 named neuralwatt/deepseek-v4.1-flash-flex for the verifier; that id is absent from the neuralwatt registry the subagent spawn path resolves (both v4.1 variants reject as Model not found while kimi-k3-flex and deepseek-v4-flash-flex spawn in probe tests), so the human selected kimi-k3-flex. The worker stays zai/glm-5.3-flash; the different-family rule holds.
E-07 [event] 2026-09-20 — T6 (td-9a444b) CLOSED on the recorded independent approval (ses_7272b6, round 1, verifier on neuralwatt/kimi-k3-flex per D-45): L-AC-01..06, Gate, the full AGENTS.md check suite in a detached clean room, tier-2 37/37, Preserved Invariants all pass (evidence /tmp/td9a444b-_.log). Worker zai/glm-5.3-flash, 5 ordered commits as PR #8, merged to dev as fcc2edd, CI success on the merge commit. Unblocks T7 and T8.
D-46 [decision] 2026-09-23 — (T2 dispatch prep, user-directed) T2 (td-9206eb) recast to agent-owned; td label, ticket header, and Spec §8 row updated together. Pre-flight passed: Gate fillable (fixture build green, 15 pages; gate assertion red pre-work as expected — /articles/ is a static route, no slug dependency) and the Interface Contract observable in dist HTML (L-AC-01..04 all assertable against built output). Verifier pinned to neuralwatt/deepseek-v4.1-flash with halt-if-not-found; spawn probe on 2026-09-23 resolved the id and returned ok, so D-45's not-found finding for the v4.1 family no longer holds on this host. Worker stays zai/glm-5.3-flash per fleet config; different-family rule holds.
D-46 [decision] 2026-09-23 — (T2 dispatch prep, user-directed) T2 (td-9206eb) recast to agent-owned; td label, ticket header, and Spec §8 row updated together. Pre-flight passed: Gate fillable (fixture build green, 15 pages; gate assertion red pre-work as expected — /articles/ is a static route, no slug dependency) and the Interface Contract observable in dist HTML (L-AC-01..04 all assertable against built output). Verifier pinned to neuralwatt/deepseek-v4.1-flash with halt-if-not-found; spawn probe on 2026-09-23 resolved the id and returned ok, so D-45's not-found finding for the v4.1 family no longer holds on this host. Worker stays zai/glm-5.3-flash per fleet config; different-family rule holds.
E-08 [event] 2026-09-23 — T2 (td-9206eb) CLOSED on the recorded independent approval (reviewer ses_f859c0, round 1, verifier on neuralwatt/deepseek-v4.1-flash per D-46): check suite all green, tier-2 loader contract exact (21 loaded, 1 skip), L-AC-01..04 verified against built output, Preserved Invariants intact. Worker zai/glm-5.3-flash in the sandbox fleet box (first fenced dispatch after the project's containerfile onboarding, see D-46; the initial subagent-tool spawn was cancelled unfenced and relaunched as a launcher-fenced headless worker). Commit c58ba09 on branch wt/td-9206eb-articles-index; full verdict archived at artifacts/t2-verification.md. Landing to dev is user-gated. Unblocks T12 partially (T12 also waits on T3, T4).
D-47 [decision] 2026-09-23 — (T3 dispatch prep, user-directed) T3 (td-d84081) recast to agent-owned; td label, ticket header, and Spec §8 row updated together (td's stored description copy keeps the creation-time frontmatter, per the T2 precedent; the ticket file is the contract source). Pre-flight: fixture build green (16 pages, unit tests 28/28), Gate assertion red pre-work as expected (/projects/ is a static route, no slug dependency). Pre-flight found one fillability gap, recast per user choice without thinning any contract line: L-AC-03 (neither summary source) had no fixture - all 3 fixture Projects carried description + ## Objective. The Interface Contract gains an explicit fixture-scope line: bench-tools drops description (Objective-only), td-orchestration drops both (neither), wikilink-resolution unchanged; the fixture edits are worker scope. Build count stays 21/22/1, so the CI grep, vault.test.ts count assertion, and nav counts remain valid. Verifier pinned to neuralwatt/deepseek-v4.1-flash with halt-if-not-found; spawn probe on 2026-09-23 resolved the id and returned PROBE-OK, matching D-46's same-day finding. Worker stays zai/glm-5.3-flash per fleet config; different-family rule holds.
E-09 [event] 2026-09-23 — T3 (td-d84081) CLOSED on the recorded independent approval (reviewer ses_28b50c, round 1, verifier on neuralwatt/deepseek-v4.1-flash per D-47): L-AC-01..04, Gate (17 pages, 21/22/1), astro check, tsc, unit 28/28, Preserved Invariants all pass on detached clean-room builds from the committed lock; evidence /tmp/td-d84081-_.log. Worker zai/glm-5.3-flash, commits 7ce3d63 (fixture scope) + a4a7fd8 (route) on branch subagents/run_mue2dwes_fim4gk/task_1; landing to dev is user-gated. Leaves T12 waiting on T4 alone. Dispatch-environment finding for future dispatches: the subagent worktree harness symlinks node_modules to the main checkout, so Tailwind's automatic source detection scans the target and emits a ~76KB pure-insertion stylesheet (normal builds: 26.4KB); worker npm ci was correctly skipped for the same reason and install integrity was covered by the verifier's clean room - build assertions only ever in detached clean rooms. Verifier notes, not findings: the empty-category fallback is source-verified only (no 0-Project fixture); lint/format:check now pass on a88529b except .docs/specs/deppfellow-shipping/artifacts/t2-verification.md (verify-skill Known items stale, maintainer's job).
D-48 [decision] 2026-09-23 - (user design review, after the T3 merge) The user reviewed the shipped T6 reading page on dev and approves its design language for T8's reuse; no new design-reference pass is needed before T8. One revision noted, explicitly not gating T8: the reading-page h1 should render larger. History per the user: body text was deliberately reduced to 1.125rem (`text-note`) because it read too big, and the h1 got carried along to the same size. Content keeps 1.125rem; the h1 moves up to title scale. It currently uses `text-note` (1.125rem, font-semibold), which is smaller than plate-row titles (`text-title`, 1.25rem). Scope any bump to the reading-page h1 class; do not mutate the `--text-title` or `--text-note` tokens, which index rows and body text share. Landing is the next orchestrator's call: a one-line class change on the T6 surface, either folded into the T8 dispatch wave or shipped as its own tiny amendment.
D-48 [decision] 2026-09-23 — (T4 dispatch prep, user-directed) T4 (td-a0a097) recast to agent-owned; td label, ticket header, and Spec §8 row updated together (td's stored description copy keeps the creation-time frontmatter, per the T2 precedent; the ticket file is the contract source). Pre-flight: fixture build green (16 pages, unit tests 28/28), Gate assertion red pre-work as expected (/logs/ is a static route, no slug dependency). Pre-flight found one fillability gap, recast per user choice without thinning any contract line: the fixture Logs could exercise neither the 240-character truncation path (all body paragraphs run under it), the heading-only no-excerpt path (all four carry body paragraphs), nor L-AC-04's divergent-date grouping (every fixture has created equal to its filename date). The Interface Contract gains an explicit fixture-scope line: 2026-09-10 keeps a short body paragraph, 2026-09-07 gains a body paragraph over 240 characters (ellipsis path), 2026-09-09 body becomes the `# ` heading only (no excerpt element), 2026-09-08 sets created: 2026-09-11 so its row groups under 2026-09-11 and its filename group disappears; the fixture edits are worker scope. Build count stays 21/22/1, so the CI grep, vault.test.ts count assertion, and nav counts remain valid. Verifier pinned to neuralwatt/deepseek-v4.1-flash with halt-if-not-found; spawn probe on 2026-09-23 resolved the id and returned PROBE-OK, matching D-46/D-47's same-day finding. Worker stays zai/glm-5.3-flash per fleet config; different-family rule holds.
E-10 [event] 2026-09-23 — T4 (td-a0a097) CLOSED on the recorded independent approval (round 1, verifier on neuralwatt/deepseek-v4.1-flash per D-48): L-AC-01..04, the fixture-scope line, and the empty-category path all pass against a detached clean-room build of f4ae2f5; Gate green (Loaded 21, exact skip warning, dist/logs/index.html); format/format:check/lint/check/tsc green; excerpt unit tests 5/5; no overflowX at 1440 and 390; Preserved Invariants pass. Evidence /tmp/t4-pr-checks.json, /tmp/t4-pr-gate.log, /tmp/t4-pr-check.log, /tmp/t4-pr-tsc.log, /tmp/t4-pr-npmci.log, /tmp/t4-pr-tests.log, /tmp/t4-empty-check.log, /tmp/t4-invariants.log, /tmp/t4-base-gate.log. Worker zai/glm-5.3-flash; PR #11 (branch subagents/run_mue3j7qg_101yao/task_1) carries the feature (src/pages/logs/index.astro, src/components/LogRow.astro, src/lib/excerpt.ts + excerpt.test.mjs) and the fixture-scope edits (09-07 >240-char paragraph, 09-09 heading-only body, 09-08 created=2026-09-11, 09-10 untouched; counts stay 21/22/1); landing to dev is user-gated. Verifier-noted, non-blocking: the PR also prettier-formats the T2 verification artifact (turns a base-red format:check green; contract silent, out of scope). T8 dispatch-prep flag from the verifier: the divergent fixture's row title links to the slug 2026-09-08 while the row groups under created 2026-09-11 — T4's contract does not pin the link date, and T8 pins detail routes to the created date, so T8's prep must reconcile the index hrefs with its route rule. Unblocks T12 (T2, T3, T4 all closed).
D-49 [decision] 2026-09-24 — (T4 recast, round 2, user-directed) T4 (td-a0a097) reopened after the user compared the built page against the Rasyid reference (artifacts/t4-shots); PR #11 had already merged, so round 2 bases on fresh dev. Recast without thinning REQ-17: (1) a vertical hairline separates the row's date column from its content column; (2) rows render no title element - the date is the log's title; (3) the date cell becomes the row's anchor, label and href bound to created, never the filename slug (resolves E-10's T8 link-date flag: the divergent fixture anchors /logs/2026-09-11/); (4) the Logs index content column holds the 65ch prose measure, a user-directed exception to DESIGN.md's list-holds-container exemption - the DESIGN.md amendment itself is the maintainer's follow-up; (5) below the small breakpoint the row stacks date-above-content and the vertical rule disappears, per DESIGN.md's locked row-rhythm collapse. Date-vs-title binding was delegated to agent judgment: created wins because grouping, ordering, the group heading, and T8's created-date routes all bind it; the title is the filename echo and diverges exactly when it is wrong. Grill declined: every sub-decision rides a locked precedent, a user directive, or a mechanical answer; the open product fork (65ch for the Articles/Projects indexes too) stays out of scope. L-AC-05 and L-AC-06 added to make the recast machine-checkable. Worker and verifier models unchanged (zai/glm-5.3-flash; neuralwatt/deepseek-v4.1-flash). Round 2 of the cap-2 review cycle.
E-11 [event] 2026-09-24 — T4 (td-a0a097) CLOSED on the recorded independent approval (round 2/2, ses_994857, verifier on neuralwatt/deepseek-v4.1-flash per D-49): L-AC-01..06 all pass against detached clean-room builds of base a9ae829 and the PR head (fresh npm ci, cleared .astro store) — 4 rows each created date once, max excerpt exactly 240 chars with ellipsis, all tag hrefs /tags/<tag>/, the divergent fixture grouped and anchored under /logs/2026-09-11/, no title element, .measure 65ch on the logs main, sm hairline + sub-sm stacked date-above-content with rule hidden confirmed by captures at 1440 and 390, an empty-Logs vault renders "No logs published yet.", counts stay 21/22/1 with the exact skip; invariants byte-identical home/articles/projects modulo CSS hash with only 4 LogRow selector additions; astro check, tsc, lint, format:check, npm ci green. Evidence /tmp/t4-r2/. Worker zai/glm-5.3-flash; round 2 required a recovery dispatch — attempt 1 (run_muexrdlt_ol14i6) staged the full implementation but was killed by an orchestrator-side session error before any commit; the staged diff was preserved at /tmp/t4-round2-staged.patch, and attempt 2 (run_muez8v10_7yunam) applied, re-verified, and delivered it unchanged as PR #12 (commits 5cc2e06 + b6c9d8e, branch subagents/run_muez8v10_7yunam/task_1). Landing to dev is user-gated. Maintainer follow-ups: DESIGN.md amendment for the logs-page 65ch exception (D-49); td's stored ticket description still carries the pre-recast contract text (creation-time copy per the T2 precedent — the .docs ticket file is authoritative; sync suggested by the verifier); the verify skill's feature map may still describe the round-1 row shape. Unblocks T12 fully (T2, T3, T4 all closed).
D-50 [decision] 2026-09-24 — (T4 recast, round 3, user-directed with explicit cap-2 override) T4 (td-a0a097) reopened a third time after the user reviewed the running page against their red-marked screenshot (artifacts/t4-shots/localhost-logs.2.png). The cap-2 breach is the user's explicit authorization, recorded here. Recast: the page becomes a single flat list ordered by created descending — no date-group headings and no bordered container elements; rows keep the D-49 anatomy (created-date cell, vertical hairline, excerpt + tags, no title, 65ch measure, sub-sm stacking). REQ-17 amended from "renders date groups newest-first" to "renders logs newest-first as a flat ruled list"; the per-row left-side date satisfies its date requirement per the user. L-AC-04 reworded from grouping to flat ordering; L-AC-07 added (flat render: no container, no headings — machine-checkable). The screenshot's red marks were ambiguous between the containers/headings and the per-row date cells; resolved by asking rather than guessing — the user confirmed dates stay as the left-side cell. T8's contract line "consistent with T4 grouping" is now stale prose; its dispatch prep must recast that line (detail routes stay created-based). Worker and verifier models unchanged (zai/glm-5.3-flash; neuralwatt/deepseek-v4.1-flash).
D-51 [decision] 2026-09-24 — (T8 dispatch prep, user-directed) T8 (td-2a7cd5) recast to agent-owned; td label, ticket header, and Spec §8 row updated together (td's stored description keeps the creation-time copy per the T2 precedent; the ticket file is the contract source). Pre-flight: fixture build green (18 pages; Loaded 21, exact broken-note skip warning), Gate red pre-work as expected (no detail routes exist), every L-AC observable in dist HTML with no fixture-scope gap (wikilink-resolution's body carries the wikilinks L-AC-04 asserts). Two recasts, no line thinned: (1) D-50's stale prose — the route line now reads "consistent with T4's `created` ordering" (detail routes stay created-based); (2) D-44's real-route precedent — the Gate cited /logs/2026-09-02/, a date no committed fixture ever had (git history verified); Gate and Examples now use real routes: Gate pins dist/logs/2026-09-11/index.html (the divergent fixture — a filename-slug build emits 09-08, so the gate subsumes L-AC-02), Examples cite /projects/wikilink-resolution/ (lede via description + Objective) and /logs/2026-09-09/ (full prev/next chain). Verifier pinned to neuralwatt/deepseek-v4.1-flash with halt-if-not-found; spawn probe 2026-09-24 returned PROBE-OK. Worker zai/glm-5.3-flash per fleet config; different-family rule holds.
D-52 [decision] 2026-09-24 — (T8 dispatch, user-directed) The user folded D-48's h1 revision into the T8 wave and ordered immediate dispatch. The T8 ticket gains the h1 contract bullet and L-AC-06 (reading-page h1 at title scale, `text-title` on the h1 class only, `--text-title`/`--text-note` tokens untouched; located at src/pages/articles/[...slug].astro:58, currently `text-note`), and the Preserved Invariant is carved accordingly ("apart from the h1 title-scale class"). Worker dispatched on zai/glm-5.3-flash (write agent, worktree branch); verifier stays neuralwatt/deepseek-v4.1-flash per D-51 (probe PROBE-OK). Worker runs no td commands; the orchestrator owns the ledger (td start, logs, handoff recorded from the worker's report). Landing to dev remains user-gated.
E-12 [event] 2026-09-24 — T8 (td-2a7cd5) CLOSED on the recorded independent approval (round 1, verifier ses_79bac0 on neuralwatt/deepseek-v4.1-flash per D-51/D-52): clean-room Gate green (25 pages, Loaded 21 with exact skip warning, dist/logs/2026-09-11), L-AC-01..06 all pass (counts 14/3/4=21, created-keyed routes with the divergent fixture only at /logs/2026-09-11/, prev/next chain correct at both ends, 65ch measure with zero unrendered brackets across the 7 new routes, no FAB, h1 at title scale with tokens untouched), metadata header contract exact (td-orchestration lede-less), format:check/lint/check/tsc exit 0 in the clean room. Evidence .docs/specs/deppfellow-shipping/artifacts/t8-verification.md. Worker zai/glm-5.3-flash (run_muf0tdks_j7sec6), branch subagents/run_muf0tdks_j7sec6/task_1 (a130ff3, ca97bc0, 7998f1d); landing to dev is user-gated. Verifier-noted: lint/format green in the archive because .agents/ is untracked (verify-skill caveat, not a regression). Maintainer flags: dev's logs index anchors /logs/2026-09-08/ by filename slug — dead post-T8 but one of four dead row hrefs on dev today; the fix is T4's unlanded round-2/3 work (PR #12, D-49/D-50); a future vault with two logs sharing a created date fails the build loudly (contract silent, accepted).
E-12 [event] 2026-09-24 — T4 (td-a0a097) CLOSED on the human pilot's direct judgment, round 3 (D-50). The user reviewed PR #13 on the running dev surface, declared it good, and declined an agent verifier for this round. Governance path, recorded verbatim for the trail: the orchestrator session and both of the user's terminals were involvement-blocked from approving (delegated mode, involved sessions cannot approve); a fresh uninvolved scribe session (ses_b2ce53) recorded the approval with --reviewed-by "deppfellow (human pilot)" — the scribe performed no judgment, only recorded the human's verdict — and td's Mode A closed the ticket in the same transaction. Implementation: PR #13 (branch subagents/run_muf0hv46_fnenzu/task_1, head f233c81), flat created-desc list, no group headings, no bordered containers, D-49 row anatomy kept; CI verify SUCCESS, Gate green, worker clean-room 19/19 assertions incl. L-AC-07 and preserved invariants. PR #12 (round 2, D-49) closed unmerged as fully superseded — content containment verified on-branch (only src/pages/logs/index.astro differs). Worker zai/glm-5.3-flash. Landing to dev is user-gated (user merges #13). Maintainer follow-ups stand (E-11, D-50): DESIGN.md amendment (65ch exception, flat-list deviation, hover token scope), td stored-description sync, verify-skill feature map. Unblocks T12 fully (T2, T3, T4 all closed).
D-53 [decision] 2026-09-24 - (T12 dispatch prep, user-directed) T12 (td-3ba9ec) recast to agent-owned; td label, ticket header, and Spec §8 row updated together (td's stored description keeps the creation-time copy per the T2 precedent; the ticket file is the contract source). Pre-flight: fixture build green (25 pages; Loaded 21 with the exact broken-note skip warning), Gate red pre-work as expected (no dist/search/index.html, no dist/pagefind/), L-AC-01..03 observable in dist HTML with L-AC-03's pre-state already clean (zero script tags on dist/index.html and dist/articles/index.html), and no fixture-scope gap: L-AC-04 is fillable against real fixture body phrases present in built HTML ("recency tax", "cannot be rebuilt from raw events" on /articles/memory-layers-for-long-horizon-agents/). RuleBand.astro mounts on every page family (home, articles, projects, logs, index and detail), so the band-control contract is assertable per page. pagefind is absent from package.json and node_modules pre-flight; the devDependency plus the post-build wiring are worker scope per the ticket Setup line. Verifier pinned to neuralwatt/deepseek-v4.1-flash with halt-if-not-found; spawn probe 2026-09-24 returned PROBE-OK. Worker zai/glm-5.3-flash per fleet config; different-family rule holds. Worker runs no td commands; the orchestrator owns the ledger (D-52). Landing to dev remains user-gated.
E-13 [event] 2026-09-24 - T12 (td-3ba9ec) CLOSED on the recorded independent approval (round 1, verifier ses_da8d18 on neuralwatt/deepseek-v4.1-flash per D-53): detached clean-room npm ci exit 0, Gate build exit 0 (26 pages, Loaded 21 with exact skip warning), L-AC-01..04 all pass (L-AC-02 anchors on 26/26 built HTML, L-AC-03 zero scripts on home/articles plus no pagefind ref outside /search, L-AC-04 via headless Chrome Pagefind.search returning /articles/deterministic-builds-beat-cached-ones/), check/tsc clean, home pixel-diff vs dev 6054728 localized to the rule-band row. Evidence .docs/specs/deppfellow-shipping/artifacts/t12-verification.md. Worker zai/glm-5.3-flash (run_muf4jqel_5vfj1x), branch subagents/run_muf4jqel_5vfj1x/task_1, PR #17 open and MERGEABLE against dev; landing is user-gated. Verifier-noted (non-blocking): no data-pagefind-body attribute, so band/nav text enters the index - contract satisfied; content-scope tuning left as follow-up with the Component UI summary-line face compromise (worker handoff).
D-54 [decision] 2026-09-24 - (T12 recast, pilot-directed, grilling session) After running the closed PR #17 implementation the pilot recast T12 from the /search route to an overlay: a band-triggered Pagefind modal plus a scoped logs searchbox. Grilled decisions, all pilot-confirmed: PR #17 closed unmerged as superseded (was OPEN and MERGEABLE, nothing shipped to revert; its branch stays locally as reusable wiring/template source); official Pagefind 1.5 Component UI (pagefind-modal, pagefind-modal-trigger, pagefind-searchbox are its prebuilt components) on searching pages, no custom lazy loader; Search trigger on exactly home plus the three category indexes - all three note-detail families (article, log, project) stay script-free with the article FAB as sole exception, extension to details is the pilot's stated fallback if inconvenient; the logs searchbox sits on /logs/ between heading and first date group and searches logs only, while the modal searches articles and projects only; results render through ruled-row custom templates in DESIGN.md language, not default Component UI rows. Index scoping: note content templates mark data-pagefind-body with data-pagefind-filter type=article|project|log; home, 404, chrome, and future tag pages stay unindexed (also resolves the round-1 verifier note on band text entering the index). Consequential spec edits: REQ-21 and SPEC-AC-06 recast; REQ-28's route set drops /search/ (scripts/gate.mjs REQUIRED_STATIC drops it in the worker PR - it is one of dev's pre-existing gate reds); the D-31 script-policy line, the search decision bullet, the verification whitelist, and the ADR-0002 narrowing note rewritten to the search-carrying-pages shape. Contract: Gate is build + dist/pagefind present + dist/search absent; L-AC-01..06 with fixture phrases recency tax (article) and one ember mark (log); copy pinned (Search / Search articles and projects / Search logs). Verifier stays neuralwatt/deepseek-v4.1-flash (same-day probe PROBE-OK, D-53); worker zai/glm-5.3-flash; worker runs no td commands (D-52); landing user-gated.
D-55 [decision] 2026-09-24 - (T12 contract erratum, mid-flight) The recast draft's Preserved Invariants said "page count 25"; the true base c6a211f count is 26 - 21 note details (14 articles, 3 projects, 4 logs), home, 3 category indexes, 404 - verified by an isolated build of c6a211f and by counting dist HTML. Root cause: 25 was the pre-T10 page count carried forward stale; T10's 404 page added one after the number was written. The worker flagged it and adapted its L-AC evidence to the binding form (no page added or removed vs base); the ticket line is amended to carry 26 with the full breakdown plus the delta as the binding assertion, committed before verifier dispatch so the verifier's ticket copy is correct. Lesson encoded: absolute page counts in contracts must state their arithmetic or state only the delta.
E-14 [event] 2026-09-24 - T12 (td-3ba9ec) recast CLOSED on the recorded independent approval (round 1, verifier ses_7b7f52 on neuralwatt/deepseek-v4.1-flash per D-54): detached clean-room npm ci exit 0, Gate build exit 0 (26 pages, Loaded 21 with exact skip warning, gate.mjs red on exactly /rss.xml + /sitemap.xml), L-AC-01..06 all pass - Search trigger (button, no href) on exactly home + 3 category indexes and none on the 21 note detail pages, zero scripts on every detail page, headless Chrome execution proof for both scoped searches (modal: recency tax returns the memory-layers article, one ember mark returns no log; logs searchbox: one ember mark returns /logs/2026-09-10/ only), ruled-row rendering on both surfaces, page set identical base-vs-branch (26), check/tsc/format:check/lint exit 0. Evidence .docs/specs/deppfellow-shipping/artifacts/t12-verification.md (replaces the v1 artifact; prettier clean). Worker zai/glm-5.3-flash (run_mufen9a0_2o30em), branch subagents/run_mufen9a0_2o30em/task_1, PR #18 open and MERGEABLE with CI SUCCESS; landing is user-gated. Verifier-noted (non-blocking): per-instance scoping presets Instance.searchFilters because configureInstance in 1.5.2 cannot express result filters (behavior contract holds); Pagefind indexes 20/21 marked pages because /logs/2026-09-09/ carries an empty body region (heading-only fixture, out of scope). Ride-along hygiene on dev this wave: recast docs and the v1 artifact prettier-formatted (759d4ff, ec497cf - format:check had been red on the unpushed v1 artifact), stray t4-shots PNGs uncommitted (b594715).
