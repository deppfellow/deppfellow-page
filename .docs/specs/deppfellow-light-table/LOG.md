E-01 [event] 2026-09-26 — grilling initialized; slug confirmed "deppfellow-light-table"; one spec covering light mode and small-width band collapse.
D-01 [decision] 2026-09-26 — Theme model: system-first on first visit (`prefers-color-scheme`), two-state toggle (light/dark, no "system" state), manual choice persisted in `localStorage`, inline pre-paint script in the head prevents a flash of the wrong theme.
D-02 [decision] 2026-09-26 — Light palette concept "the light table" (the plate held against cold light): cold paper ground, never cream; prose ink becomes a near-black cold blue; star, rule, and plate-edge darken for legibility; the Two-Ink Rule and One Ember Rule carry over unchanged; iris stays interaction-only, ember stays the recency mark. DESIGN.md gains a dual-world token section; exact swatches come back for approval before tickets.
D-03 [decision] 2026-09-26 — Band collapse below 640px: the band shows site name plus a "Menu" trigger (Scales caps, matching the FAB's "Index" trigger style); the open menu holds the category links with counts, Search, and the theme toggle; the expanded panel is FAB-style: full width, dropping from the band, hairline-bounded with plate-edge stacked edges, closing on Escape, outside click, and navigation. Above 640px the band is unchanged except the theme toggle inserted left of Search.
D-04 [decision] 2026-09-26 — Theme toggle is a sun/moon icon with destination semantics: sun while dark (press goes light), moon while light (press goes dark); placed left of Search where Search renders, otherwise the last item on the band's right; a deliberate single exception to the no-icon band vocabulary; accessible name states the action.
D-05 [decision] 2026-09-26 — Light-table palette approved: plate #f3f5f9, plate-edge #e8ecf2, rule #ccd3e0, rule-strong #9fabbe, bone-as-ink #0c1220, star #4d6087, iris #3a6bce, ember #e0612e; same token slots redefined per world so component code is unchanged; implementation runs a WCAG contrast pass and reports any forced deltas.
D-06 [decision] 2026-09-26 — Derived with D-05: `color-scheme` flips per world; selection becomes iris-mix with ink text; Pagefind's `--pf-*` follow the tokens; the one hard-coded `--pf-mark: #ffffff` becomes a token; theme switches instantly (One-Step Rule, no fade); while on system default the page follows live `prefers-color-scheme` changes, and a stored manual choice suppresses them.
D-07 [decision] 2026-09-26 — Documentation packaging: DESIGN.md gains the light-table values and a dual-world section with every existing rule carried over verbatim "in either world"; ADR-0015 "Light table: a dual-world palette" records the concept, the cold-not-warm refusal, the system-first persisted choice, and the same-slot token strategy; CONTEXT.md gains "Light table" and the Rule band entry gains the Menu trigger.
D-08 [decision] 2026-09-26 — Menu closure: any activation closes the panel (category link, Search, theme toggle); Escape and outside click also close, matching the FAB.
D-09 [decision] 2026-09-26 — Script consequence of D-01/D-03: the shipping spec's page-script whitelist (its D-17) extends to include the theme pre-paint inline script (head, every page) and the band menu script (every page with the band); reading pages keep the FAB alongside these, not instead of it.
D-10 [decision] 2026-09-26 — Toggle icon drawing: a thin line icon, ~16px, 1.5px stroke, currentColor, no fill; recolours with hover exactly like every other band trigger.
D-11 [decision] 2026-09-26 — Inside the open mobile menu the theme control is a full-width row: line icon plus destination word ("Light"/"Dark"), matching the category rows.
D-12 [decision] 2026-09-26 — No-JS fallback: the band's markup renders as today (links visible, wrapped); a tiny script sets an attribute on `<html>` and CSS performs the Menu collapse only in that enhanced state. Scripts off leaves the current band.
E-02 [event] 2026-09-26 — derivation gate: frontier empty, human confirmed shared understanding (confirmed by invoking /draft-spec).
E-03 [event] 2026-09-26 — council skipped by explicit human instruction ("skip council"); quiz gate proceeds on the un-reviewed draft.
Q-01 [question] 2026-09-26 — ADR-0002's "ordinary pages stay script-free" line vs D-09's theme pre-paint script and band menu script: read as a further narrowing in the D-16 spirit (tiny, scoped, no runtime; amend ADR-0002 with a supersedes citation at resolution) or re-grill? Owner: human; revisit: quiz gate.
D-13 [decision] 2026-09-26 — Q-01 resolved: D-09's theme pre-paint script and band menu script are a further narrowing of ADR-0002 in the D-16 spirit (tiny, scoped, no runtime); that ADR's ordinary-pages script-free line is superseded in part by this initiative's ADR-0015 (supersedes ADR-0002 in part; ADR-0002 carries a superseded-by line at resolution).
D-14 [decision] 2026-09-26 — ADR-0013 ruled "extended, not reversed": the dark world stays default and unchanged; the light table adds a second lighting of the same visual world; no conflict machinery applies.
E-04 [event] 2026-09-26 — spec approved at quiz gate (human: "approved"): ticket cut accepted, council-skip standing, Q-01 and ADR-0013 ruled; contract re-derived as approved; tickets and td wiring generated.
D-15 [decision] 2026-09-26 — Contrast pass amendment of D-05 (REQ-03 mechanism, first exercised): the human ruled the light-world hairline pairs enforce a hard 3:1 floor, which the approved values failed (rule #ccd3e0 at 1.38:1, rule-strong #9fabbe at 2.13:1; dark-world hairlines at 1.27:1/1.61:1 stay byte-identical and report-only). Amended light values: rule #7f8ca1 (3.12:1), rule-strong #748096 (3.65:1), same cold hue family; hierarchy under star (5.76:1) and bone (17.1:1) preserved. Approved by the human in the execution session; amends D-05, never a silent change.
