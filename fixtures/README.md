# Fixtures

Synthetic sample notes used for local development and for screenshot review. They exist because the public vault's categories are empty, and a surface cannot be judged without content in it.

Nothing here is published content. The real corpus lives in the vault and reaches the build through `WIKI_PATH`. Delete or ignore this folder once the public categories hold real notes.
